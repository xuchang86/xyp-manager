<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="http://www.springframework.org/schema/beans 
						http://www.springframework.org/schema/beans/spring-beans-3.0.xsd">

	<!-- 定时任务执行类 -->
	<!-- <bean id="labJob" class="com.rogrand.job.LabJob"></bean>
	
	定时器配置
	<bean id="jobDitail" class="org.springframework.scheduling.quartz.MethodInvokingJobDetailFactoryBean">
		<property name="targetObject" ref="labJob"></property>指定任务类
		<property name="targetMethod" value="countPrice"></property>指定任务方法
	</bean>
	
	定义时间间隔触发器
	<bean id="timeTigger" class="org.springframework.scheduling.quartz.CronTriggerBean">
		<property name="jobDetail" ref="jobDitail"></property>
		<property name="cronExpression">
			<value>00 44 19 * * ? </value>每天早上8点执行一次
		</property>
	</bean>
	
	启动定时器
	<bean id="startJob" class="org.springframework.scheduling.quartz.SchedulerFactoryBean">
		<property name="triggers">
			<list>
				<ref bean="timeTigger"/>  
			</list>
		</property>
	</bean> -->

</beans>
